---
name: No Agent tool for implementation tasks
description: User rejects Agent tool when used for writing code or making file changes
type: feedback
---

Do not use the Agent tool for implementation tasks (writing files, editing code, running tests).
Only use direct tools (Write, Edit, Bash, Read, Grep, Glob) for all coding work.

**Why:** User rejected the Agent tool call when it was about to implement Phase 3/4, tests, and README.

**How to apply:** Reserve Agent/Explore only for pure research/exploration (reading large codebases). All writing, editing, and running must be done directly.
